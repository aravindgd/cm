.container
  %p#notice= notice

  %p
    %b Name:
    = @ad.name
  %p
    %b Content:
    = @ad.content
  %p
    %b Category:
    = @ad.category.name

  %p
    %b Image:
    - if @ad.ad_image_attachments
      .image-slider
        - @ad.ad_image_attachments.each do |ad_image|
          = image_tag(ad_image.image_url(:thumb))

  %hr

  = link_to 'Edit', edit_ad_path(@ad), class: "btn btn-warning"
  \|
  = link_to 'Back', ads_path, class: "btn btn-success"
