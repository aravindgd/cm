.container
  %h1 Listing ads

  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Content
        %th Image
        %th Category
        %th
        %th
        %th
    %tbody
      - @ads.each do |ad|
        %tr
          %td= ad.name
          %td= ad.content
          %td= image_tag(ad.image_url) if ad.image?
          %td= link_to 'Show', user_ad_path(current_user, ad)
          %td= link_to 'Edit', edit_user_ad_path(current_user, ad)
        -# %td= link_to 'Destroy', user_ad_path(current_user), :method => :delete, :data => { :confirm => 'Are you sure?' }

  %br

  = link_to 'New Ad', new_user_ad_path(current_user)
