= simple_form_for(@ad) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :content
    = f.input :image
    = f.input :belongs_to

  .form-actions
    = f.button :submit
